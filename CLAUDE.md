@AGENTS.md

## Claude Code / 에이전트 진입 규칙

- 먼저 `AGENTS.md`, `SCHEMA.md`, `index.md`, 최근 `log.md`를 읽고 작업한다.
- `bookclub/`, `script/`, `_archive/wiki_v1/`는 원본/아카이브로 취급하고 수정하지 않는다.
- v2 문서는 원본에서 새로 작성한 뒤, 필요한 경우 v1과 비교한다.
- 여러 파일 수정 전에는 계획과 대상 파일 목록을 제시한다.
- 변경 후에는 생성/수정 파일 목록과 다음 작업 후보를 보고한다.
